#include<stdio.h>
int main()
{
int num;
printf("Enter natural number:");
scanf("%d",&num);
if(num>0)
printf("\n Enter the posstive value :");
if(num<0)
printf("\n Enter the native value :");
if(num==0)
printf("\n Enter the neutral value :");
return 0;

}	
